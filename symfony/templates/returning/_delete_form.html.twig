<form method="post" action="{{ path('app_returning_delete', {'id': returning.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ returning.id) }}">
    <button class="btn">Delete</button>
</form>
